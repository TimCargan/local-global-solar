from setuptools import setup

if __name__ == "__main__":
    setup()

# deps = ["tensorflow", "tensorflow_io", "tensorflow_decision_forests", "keras-tuner", "pandas", "scikitlearn", "pyarrow"]
# pkgs = [package for package in find_packages() if package.startswith("zephyrus")]
# setup(
#     name="zephyrus",
#     packages=pkgs,
#     install_requires=deps,
#     version="0.0.1-alpha",
#     description="PhD Reaearch, Not a lib but helpful to have a setup to get all the deps installed etc",
#     author="timcargan",
#     license="None",
#     python_requires='>3.7.0'
# )